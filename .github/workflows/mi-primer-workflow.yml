name: Mi primer workflow
on:
  push:
    branches: [master]

jobs:
  mi_primer_job:
    name: Mi primer job
    runs-on: ubuntu-latest
  steps:
    - name: Imprimir
      env:
        MI_VARIABLE: Hola mundo
      run:
        echo $MI_VARIABLE
